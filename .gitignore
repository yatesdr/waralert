# Compiled binary
/waralert
/build/

# Runtime config (contains secrets: session keys, passwords, API keys)
waralert.yaml

# TLS certificates (auto-generated)
cert.pem
key.pem

# Runtime logs
audit.log

# macOS
.DS_Store

# Claude Code local settings
.claude/
